📘 EMACS: Evolutionary Multi-Agent Coding System (v3.1)Date Updated: 2026-01-26Architecture: Agentic Tool-Use + MCP-like Execution LoopCore Philosophy: Local-First, Autonomous, Self-Correcting1. 🌟 ภาพรวมระบบ (System Overview)EMACS คือระบบ AI Software Engineer ที่ทำงานบนเครื่อง Local ทั้งหมด โดยใช้โมเดลภาษาขนาดเล็ก (SLM) เช่น gemma2 หรือ llama3 มาทำงานร่วมกันเป็นทีม (Swarm) เพื่อแก้โจทย์ทางโปรแกรมมิ่งที่ซับซ้อนจุดเด่นสำคัญ (Key Capabilities)Agentic Tool Use: AI ไม่ได้แค่เขียนโค้ด แต่สามารถใช้เครื่องมือ (Search, File Read) เพื่อหาข้อมูลก่อนวางแผนได้ (ReAct Pattern)Robust Execution: รันโค้ดใน Docker Container ที่ปลอดภัย และสามารถติดตั้ง Library (pip install) ได้เองเมื่อจำเป็นSelf-Correction: มีระบบ Critic ที่คอยตรวจจับ Error (รวมถึง Silent Error) และส่ง Feedback กลับไปแก้โค้ดวนลูปจนกว่าจะผ่านDual Memory:Machine Memory: เก็บ Vector ลง ChromaDB เพื่อค้นหา Skills เก่าHuman Memory: บันทึกเป็นไฟล์ Markdown (.md) ลง Obsidian Vault เพื่อให้มนุษย์อ่านเข้าใจ2. 🏗️ สถาปัตยกรรม (Architecture)ระบบแบ่งการทำงานออกเป็น 2 Loop หลัก ที่ถูกควบคุมโดย Orchestrator:Phase 1: The Research Loop (ReAct)Agent: PlannerLogic:รับ Objective จากผู้ใช้ตรวจสอบ Memory เดิม (Librarian)ตัดสินใจว่าต้องใช้ Tool หรือไม่?ถ้าใช้: เรียก ToolRegistry -> Custom Tools -> ได้ผลลัพธ์ -> วนกลับมาคิดใหม่ถ้าพอแล้ว: สร้าง Execution Plan (JSON Steps)Tools: web_search (SearXNG), read_project_file, list_filesPhase 2: The Coding Loop (MCP-like)Agent: Coder & CriticLogic:Coder: เขียนโค้ดตาม StepSandbox: รันโค้ดใน Docker (ติดตั้ง dependencies อัตโนมัติ)Critic: ตรวจสอบผลลัพธ์Pass: บันทึกผล -> ไป Step ถัดไปFail: วิเคราะห์ Error (Line extraction) -> ส่งกลับไปแก้ (Retry Max 3 ครั้ง)Analyst: หากล้มเหลวครบโควต้า จะวิเคราะห์หาสาเหตุ (Root Cause Analysis)3. 🧩 ส่วนประกอบระบบ (Components & Files)📂 Directory Structure/emacs-project
├── src/
│   ├── core/
│   │   ├── orchestrator.py   # Main Controller (Tool Loop + Code Loop)
│   │   ├── llm.py            # Ollama Client (Instructor/Pydantic)
│   │   ├── models.py         # Data Structures (Plan, Step, CodeOutput)
│   │   ├── config.py         # Config Loader
│   │   └── sandbox.py        # Docker Execution Wrapper
│   ├── agents/
│   │   ├── planner.py        # วางแผน + ตัดสินใจใช้ Tool
│   │   ├── coder.py          # เขียนโค้ด + Clean Markdown
│   │   ├── critic.py         # ตรวจสอบ Output + Detect Silent Error
│   │   └── analyst.py        # วิเคราะห์ความล้มเหลว
│   ├── tools/
│   │   ├── registry.py       # ระบบลงทะเบียน Tool + Smart Arg Mapping
│   │   └── custom_tools.py   # ฟังก์ชัน Web Search, File Ops
│   ├── config/
│   │   └── prompts.yaml      # System Prompts สำหรับ Agent
│   └── memory/
│       └── librarian.py      # ChromaDB + Markdown Log Logic
├── workspace/                # พื้นที่สำหรับวางไฟล์โค้ดที่ AI สร้าง (Mounted to Docker)
├── memory_logs/              # พื้นที่เก็บ Obsidian Markdown (Human Memory)
├── genome_config.json        # ตั้งค่าระบบ (Model names, Paths)
├── docker-compose.yml        # SearXNG, Langfuse
└── main.py / app.py          # Entry Points (CLI / GUI)
4. 🛠️ Tech Stack & DependenciesComponentTechnologyRoleBrainOllamaรัน Local LLM (OpenAI API Compatible)Logic ControlPythonภาษาหลักของระบบValidationPydantic + Instructorบังคับ Output JSON ให้ตรง SchemaSandboxDocker SDKรันโค้ด Python ในสภาพแวดล้อมที่แยกขาดKnowledgeChromaDBVector Store สำหรับ RAGToolsSearXNGPrivate Search Engine (Self-hosted @ Port 8081)GUIStreamlitDashboard สำหรับสั่งงาน5. 🧠 รายละเอียด Agent Logic (Deep Dive)Planner (src/agents/planner.py)ความสามารถ: เข้าใจ ToolRegistryInput: Objective + ContextOutput: Plan Object ที่มีรายการ StepPrompt Trick: ถูกสอนให้ระบุ tool_needed และ description (ที่เป็น args) อย่างชัดเจนCoder (src/agents/coder.py)ความสามารถ: เขียนโค้ด, ระบุ Dependencies, ทำความสะอาด MarkdownSafety:มีฟังก์ชัน clean_markdown ลบ ```python ออกตรวจสอบชื่อไฟล์ filename ไม่ให้เป็น path ว่าง หรือ DirectoryCritic (src/agents/critic.py)ความสามารถ: แยกแยะ Success/Fail และตรวจจับ Silent ErrorSmart Logic:ถ้า Exit Code 0 แต่ Output ว่าง -> Fail (Silent Error)ใช้ Regex ดึง line (\d+) จาก Traceback เพื่อชี้เป้าบรรทัดที่ผิดให้ CoderOrchestrator (src/core/orchestrator.py)ความสามารถ: เป็นสมองกลาง เชื่อมต่อทุก AgentFeature เด็ด:Auto-Install: สแกนหา ModuleNotFoundError แล้วสั่ง pip install ใน Sandbox อัตโนมัติArgument Mapping: ช่วยแก้ปัญหา AI ส่ง parameter ผิดให้ Tool (ใน registry.py)6. ⚙️ Configuration (genome_config.json)ไฟล์นี้เปรียบเสมือน DNA ของระบบ สามารถปรับเปลี่ยนได้โดยไม่ต้องแก้โค้ด{
  "system_name": "EMACS_v3.1",
  "obsidian_vault_path": "./memory_logs",  // Path เก็บ Markdown Skill
  "max_attempts_per_step": 3,              // จำนวนครั้งที่ยอมให้แก้โค้ด
  "models": {
    "planner": "gemma2",
    "coder": "gemma2",     // แนะนำ: deepseek-coder-v2 หากเครื่องไหว
    "critic": "gemma2",
    "analyst": "gemma2"
  }
}
7. 🚀 วิธีการรัน (Quick Start)Start Services:docker-compose up -d
ollama serve
Run GUI:streamlit run app.py
Command Flow:User กรอก Objectiveระบบเริ่ม Research -> Plan -> Code -> Test -> Save Memoryดูผลลัพธ์และโค้ดได้แบบ Real-time บนหน้าเว็บเอกสารนี้จัดทำขึ้นเพื่อให้ AI สามารถอ่านแล้วเข้าใจบริบทของ EMACS Project ได้ทันที เพื่อการพัฒนาต่อยอดหรือ Debug ในอนาคต
